## Setup

```sh
make install
```

## Start Example Servers

```sh
# Flask
(cd examples/framework-flask && make dev)
```

## Test

Run all the things:

```sh
make precommit
```

Run things individually:

```sh
make format
make lint
make type-check
```
